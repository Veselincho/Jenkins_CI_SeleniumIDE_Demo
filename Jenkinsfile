pipeline {
    agent any

    stages {
        stage("Checkout code") {
            steps {
                //Checkout the repository
                git branch: 'main', url: 'https://github.com/Veselincho/Jenkins_CI_SeleniumIDE_Demo'
            }
        }
    }
}
